using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
namespace HardlyBrief.Movement {

    [RequireComponent(typeof(MovementController))]
    public class MovementControllerHUD : MonoBehaviour
    {
        protected MovementController _movementController;

        void Awake(){
            _movementController = this.GetComponent<MovementController>();
        }

        protected virtual void OnGUI(){
            GUI.color = Color.green;

            GUI.Label(new Rect(Screen.width - 310f,0,300,20), $"Movement Controller Info");
            GUI.Label(new Rect(Screen.width - 310f,20,300,20), $"Current Speed {Mathf.Round(_movementController.CurrentSpeed)}");
            GUI.Label(new Rect(Screen.width - 310f,40,300,20), $"Grounded?: {_movementController.IsGounded}");
            GUI.Label(new Rect(Screen.width - 310f,60,300,20), $"Sprinting?: {_movementController.IsSprinting}");
        }
    
    }
}
*/
